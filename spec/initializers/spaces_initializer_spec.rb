require "rails_helper"

RSpec.describe "Spaces Initializer" do
  xit "fetches spaces information and caches it" do
    # 
  end

  xit "creates tags and saves them to the database" do
    # 
  end
end