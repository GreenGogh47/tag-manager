<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Bootstrap demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>

  <%# Begin Shared Tags %>
  <div class="card mt-4">
    <div class="card-header">
      <h5 class="card-title">Shared Tags</h5>
    </div>
    <div class="card-body">
      <ul>
        <% @spaces.shared_tags.each do |tag| %>
          <button type="button" class="btn btn-light custom-text-color" style="background-color: <%= tag.tag_bg %>;">
            <%= tag.name %>
          </button>
        <% end %>
      </ul>
    </div>
  </div>

  <%# Columns %>
  <div class="container text-center p-5 mb-5">
    <div class="row row-cols-2">

      <%# Cards %>
      <% @spaces.spaces.each do |space| %>
        <div class="col">
          <div class="card mb-4">
            <div class="card-header" style="background-color: <%= space.color %>;">
              <div class="d-flex justify-content-between align-items-center">
                <h3 class="card-title text-white"><%= space.name %></h3>
                <small class="text-muted text-right"><%= space.hidden? %></small>
              </div>
            </div>

            <%# Members %>
            <div class="card-body">
              <h6 class="card-subtitle text-muted">Members: <%= space.all_members %></h6>
            </div>

            <%# Statuses %>
            <div class="card-footer">
              <h6 class="card-subtitle mb-2 text-muted">Statuses</h6>
              <ul>
                <% space.statuses.each do |status| %>
                  <button type="button" class="btn btn-light" style="background-color: <%= status.color %>;">
                    <%= status.name %>
                  </button>
                <% end %>
              </ul>
            </div>

            <%# Tags %>
            <div class="card-footer">
              <h6 class="card-subtitle mb-2 text-muted">Tags</h6>
              <ul>
                <% @spaces.unique_tags(space).each do |tag| %>
                  <div class="btn-group">
                    <button type="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" style="background-color: <%= tag.tag_bg %>; color: black; border: 1px solid black;">
                    <%= tag.name %>
                  </button>
                    <ul class="dropdown-menu">

                      <%# Update other dropdown items to be a form. %>
                      
                      <li>
                        <%= form_tag(tag_path(tag), method: :patch, data: {
                          controller: "colors"
                        }) do %>
                          <div class="dropdown-item">
                            <%= submit_tag "Update" %>
                          </div>
                        <% end %>
                      </li>

                      <li><hr class="dropdown-divider"></li>
                      <li><a class="dropdown-item" href="#">Match to this Color</a></li>
                      <li><hr class="dropdown-divider"></li>
                      <li><a class="dropdown-item" href="#">Make this Tag a "Shared Tag"</a></li>
                      <li><hr class="dropdown-divider"></li>
                      <li><a class="dropdown-item" href="#">Delete (Can't Be Undone!)</a></li>
                    </ul>
                  </div>
                <% end %>
              </ul>
            </div>
            <%# End Tags %>
          </div>
        </div>
      <% end %>
      <%# End Spaces Cards %>
    </div>
  </div>
  
  <%# Bootstrap Bundle with Popper %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>